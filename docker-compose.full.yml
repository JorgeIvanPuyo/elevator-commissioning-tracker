services:
  web:
    build:
      context: ./apps/web
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
      NEXT_PUBLIC_APP_NAME: Megapolis Elevator Traceability
    ports:
      - "3000:3000"
    depends_on:
      - api
